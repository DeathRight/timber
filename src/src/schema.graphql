### This file was generated by Nexus Schema
### Do not make changes to this file directly


"""BigInt type"""
scalar BigInt

"""Date type, serialized as milliseconds since epoch"""
scalar Date

interface Node {
  """Snowflake ID (BigInt) of a resource"""
  id: BigInt!
}

type Query {
  userById(
    """UUID of the user"""
    id: ID!
  ): User
  userByName(
    """Display Name of the user"""
    displayName: ID!
  ): User
}

type User implements Node {
  """URL for user's avatar"""
  avatar: String

  """Time in milliseconds since epoch the user was created at"""
  createdAt: Date!

  """Whether or not the user's account is disabled"""
  disabled: Boolean

  """Global display name"""
  displayName: String!

  """Array of user IDs the user is friends with"""
  friends: [BigInt]

  """Array of room IDs for group chats the user is a member of"""
  groupchats: [BigInt]

  """Snowflake ID (BigInt) of a resource"""
  id: BigInt!

  """Time in milliseconds since epoch the user was last logged in"""
  lastSeen: Date

  """Array of server IDs the user is a member of"""
  servers: [BigInt]
}